package model;

/**
 * Defines the user roles available in the GCash ShareALoad application.
 * Currently, there are two roles: ADMIN and USER.
 * These roles can be used to implement access control and authorization features.
 */
public enum UserRole {
    ADMIN, USER;
}
